badge.textContent = s==='complete'?'✓':s==='processing'?'◐':s==='error'?'✕':'○';
if (!/\S/.test(t)) { texts.push('\0'); return; } // word boundary sentinel
if (tok === '\0' || tok === null) { // word boundary
var bannerText = 'FORENSIC INDICATORS FLAGGED — SEE SEALED REPORT';
